'use strict';

var React = require('react');

var APP = React.createClass({
  render: function(){
    return (
      <p>THIS IS THE REACT or is it? IT IS! is it though?!</p>
      );
  }
})

module.exports = APP;